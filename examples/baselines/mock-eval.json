{
  "provider": "mock",
  "model": "mock-cleaner",
  "suite": "examples/eval_cases.json",
  "expected_passed": true,
  "notes": "Deterministic CI baseline. Regenerate with librarian eval examples/eval_cases.json --output examples/baselines/mock-eval.json only when output schema changes."
}
